# Frontend Environment Variables

# API URL (optional - if not set, app runs in demo mode)
# For local development:
# VITE_API_URL=http://localhost:5000/api

# For production with Cloudflare Worker:
# VITE_API_URL=https://your-worker.your-subdomain.workers.dev/api

# Leave empty or comment out to run in demo mode (client-side only)
VITE_API_URL=
